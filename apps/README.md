# Apps

Executable benchmark apps live here.

## Current apps

- `apps/claimer`: submits and settles benchmark claims.
- `apps/challenger`: challenges benchmark claims on divergence.

These are intentionally small starter binaries so scenario wiring can move from
docs-only to runnable entrypoints.
